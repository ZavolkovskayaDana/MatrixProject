# Лабораторная работа №2
## Реализация улучшенной анимации «Матрица» с зигзагообразным движением (C++ / WinAPI)

### Описание работы
Вторая лабораторная работа расширяет функциональность проекта из первой лабы.
К классической анимации «падающих символов» были добавлены:
  - зигзагообразное движение строк (вверх–вниз);

  - ограничение движения по безопасным границам консоли;

  - корректное удаление строки, когда её «голова» достигает правого края;

  - тайминг-менеджмент через canStep() — теперь каждая строка движется с собственной скоростью и НЕ зависит от частоты кадров;

  - улучшенная логика генерации начальной позиции.

Работа реализована поверх кода из develop, но имеет собственную логику движения и анимации.

### Основной функционал
- Заполнение консоли «потоками» символов.
- Настраиваемая скорость обновления.
- Настраиваемая длина колонн.
- Режим эпилепсии (усиленные цветовые эффекты).
- Настраиваемая частота линий.
- Проверка корректности входных данных как в диалоге, так и в командной строке.
- Структура кода разделена по классам.
- Зигзагообразное движение.
- Безопасная зона по вертикали.
- Плавное появление строки.
- Плавное исчезновение.
- Независимая скорость для каждой линии.
- Полная интеграция с системой классов из первой лабы.

Работа разделена на классы и использует ту же архитектуру, что lab-01.

### Технологии
- **C++17**
- **WinAPI** (работа с консолью)
- **SystemUtils + AppManager** для управления приложением
- **Git / GitHub** для контроля версий
- **IDE: Visual Studi**

### Состав проекта
- `main.cpp` — точка входа программы, выбор режима запуска и управление параметрами.
- `Line.h / Line.cpp` — реализация «потоков» символов, формирование и движение строк.
- `Symbol.h / Symbol.cpp` — работа с отдельными символами, генерация и обновление отображаемых знаков.
- `SystemUtils.h / SystemUtils.cpp` — функции системного уровня (работа с вводом, консолью, утилитарные проверки).
- `AppManager.h / AppManager.cpp` — управление жизненным циклом строк, запуск анимации.

### Инструкция по сборке и запуску
1. Скомпилировать проект в Visual Studio (WinAPI).  
2. Запуск в диалоговом режиме:  
matrix.exe
3. Запуск в командной строке:  
3. Запуск в командной строке:  
matrix.exe <скорость 1–30> <длина 1–30> <Y/N>

### Итог
Данная ветка `laba-01` представляет готовую и проверенную версию первой лабораторной работы.  
Разработка велась в ветке `develop`, а затем перенесена сюда без изменений в структуре.
